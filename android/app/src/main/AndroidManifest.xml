<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.parking">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/dora3"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.notifications.Notifications" />
        <activity android:name=".ui.findParking.FindParking" />
        <activity android:name=".ui.newParking.NewParkingSpace" />
        <activity android:name=".ui.showParkingSpace.ShowParkingSpace" />
        <activity android:name=".ui.viewOneVehicle.ViewOneVehicle" />
        <activity android:name=".ui.viewVehicles.ViewVehicles" />
        <activity android:name=".ui.signup.SignUp" />
        <activity android:name=".ui.homescreen.HomeScreenActivity" />
        <activity android:name=".ui.login.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.viewUser.UserProfile" />
    </application>

</manifest>